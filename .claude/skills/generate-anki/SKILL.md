---
name: generate-anki
description: Generate an Anki CSV from a markdown notes file using the generate-anki script
---

Run the following command to generate the Anki flashcard CSV:

```bash
npm run generate-anki -- $ARGUMENTS
```

Report how many cards were generated and where the output file was written.
